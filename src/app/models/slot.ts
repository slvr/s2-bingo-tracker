export class Slot {

    name: string;
    slot: string;
    colors: string;

    constructor(name: string, slot: string, colors: string) {
        this.name = name;
        this.slot = slot;
        this.colors = colors;
    }

}
